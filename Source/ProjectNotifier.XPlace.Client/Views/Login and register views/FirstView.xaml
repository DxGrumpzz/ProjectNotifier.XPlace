<UserControl x:Class="ProjectNotifier.XPlace.Client.FirstView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectNotifier.XPlace.Client"
             
			 mc:Ignorable="d" 
			 
             d:DesignHeight="450" 
			 d:DesignWidth="800"
			 
			 Background="White">

	<Grid>

		<!-- Slightly faded out background -->
		<Grid.Background>
			<SolidColorBrush Color="Black" Opacity="0.2"/>
		</Grid.Background>


		<!-- Main border around the control -->
		<Border CornerRadius="12"  
				Background="White"
				
				MaxWidth="200"
				MaxHeight="250">

			<!-- Drop shadow -->
			<Border.Effect>

				<DropShadowEffect Color="#000"
								  Opacity="0.5" 
								  BlurRadius="10"
								  							    
								  RenderingBias="Quality"
								  							    
								  Direction="270"
								  								  
								  ShadowDepth="1"/>
			</Border.Effect>

			<!-- A view presentor for the login or registration views-->
			<ContentPresenter x:Name="ViewPresenter"/>

		</Border>
	</Grid>
</UserControl>
