<UserControl x:Class="ProjectNotifier.XPlace.Client.SettingIconView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectNotifier.XPlace.Client"
             mc:Ignorable="d" 
         
             d:DesignHeight="450" 
             d:DesignWidth="800"
             
             d:DataContext="{x:Static local:SettingIconViewModel.DesignInstance}">

    
    <StackPanel Cursor="Hand"
                
                Background="Transparent"
                >

        <!-- The settings icon -->
        <Image Source="{Binding Icon, 
               Converter={local:SettingIconToImageValueConverter}}"
               
               MaxWidth="30"
               MaxHeight="30"/>

        <!-- The settings description -->
        <TextBlock Text="{Binding Description}"
                   FontSize="15"
                   FontFamily="Open Sans Hebrew"
                   FontWeight="Light"
                   />

    </StackPanel>
</UserControl>
